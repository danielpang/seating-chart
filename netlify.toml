[functions]
  external_node_modules = ["express"]
  node_bundler = "esbuild"
[[redirects]]
  force = true
  from = "/api/*"
  status = 200
  to = "/.netlify/functions/api/:splat"
[dev]
  targetPort = 3000 # react app's port
  port = 8888 # netlify dev server port
